import styles from './navbar.module.scss';

const Navbar = () => {
  return (
    <div className={styles.container}>
      <button className={styles.dark}>darck Modde</button>
    </div>
  );
};

export default Navbar;
